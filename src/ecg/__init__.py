"""Fixed-patch vs RR/beat tokenization for self-supervised ECG representation learning.

Research code for the PTB-XL tokenizer comparison. Exploratory notebooks live in
``notebooks/`` and import from this package rather than redefining logic, so that
every number in a notebook is produced by the same code path as the experiments.
"""

__version__ = "0.1.0"
